name: Demo CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1 - Checkout Code
        uses: actions/checkout@v4

      - name: Step 2 - Print Message
        run: echo "Hello Ajay, GitHub Actions working!"

      - name: Step 3 - Show Files
        run: ls -la
